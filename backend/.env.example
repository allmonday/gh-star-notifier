# ===== VAPID Keys (Web Push) =====
# Generate these automatically on first run, or use a VAPID key generator
# https://vapidkeys.com/
VAPID_PRIVATE_KEY=your_vapid_private_key_here
VAPID_PUBLIC_KEY=your_vapid_public_key_here

# VAPID subject (must be a mailto: or https: URL)
VAPID_SUBJECT=mailto:your-email@example.com

# ===== GitHub Webhook Configuration =====
# Webhook secret for HMAC signature verification
# Generate a strong random string: openssl rand -hex 32
WEBHOOK_SECRET=your_webhook_secret_here

# Repository whitelist (JSON array)
# Only repositories in this list can trigger notifications
# Example: ["tangkikodo/my-repo", "owner/another-repo"]
WEBHOOK_WHITELIST=["owner/repo"]

# ===== Server Configuration =====
HOST=0.0.0.0
PORT=8000

# ===== Database =====
# SQLite database path (relative to backend directory)
DATABASE_URL=star_notifier.db
